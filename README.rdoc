= entrance - penetration testing framework for web applications
Write security tests in Gherkin.

== Installation
Make sure required gems are installed
  bundle install

== Running
To start the tests, cd into the entrance directory an run
  target=example.com bundle exec cucumber
